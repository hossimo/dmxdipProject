android {
    signingConfigs {
        release {
            storeFile file("../../keystore")
            storePassword "<redacted>"
            keyAlias "dmxdip"
            keyPassword "<redacted>"
        }
    }
}